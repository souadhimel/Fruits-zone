import React from 'react';

const Blogs = () => {
    return (
        <p>
            <h2>1. Block,Inline, Inline-block</h2>
            Block Elements: A block element always starts on a new line, and fills up the horizontal space left and right on the web page. Margins and padding can be added on all four sides of any block element — top, right, left, and bottom. Some examples of block elements are div and p tags.
        Inline Elements: Inline elements don’t start on a new line, they appear on the same line as the content and tags beside them. Some examples of inline elements are span , strong, and img tags.Inline-Block: Inline-block elements are similar to inline elements, except they can have padding and margins added on all four sides.  
        
        <h2>2. Semantic Tag</h2>
        <p>A semantic element clearly describes its meaning to both the browser and the developer. form , table , and article - Plainly defines its content and they are semantic tags.</p>
        
        
        
        </p>
    );
};

export default Blogs;